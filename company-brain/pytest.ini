[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
asyncio_mode = auto
markers =
    slow: marks tests as slow (>2s) — excluded from `make test-fast`
filterwarnings =
    ignore::DeprecationWarning:pydantic.*:
    ignore::PendingDeprecationWarning
